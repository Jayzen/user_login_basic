<% provide(:title, t('resume_settings')) %>
<div class="row">
  <%= render 'shared/resume_bar' %>
  <div class="col-md-10">
    <div class="card">
      <div class="card-header text-secondary text-center">
        <%= t('resume_settings') %>
      </div>
    </div>
  </div> 
</div>
