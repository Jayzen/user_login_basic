<nav class="navbar navbar-expand-sm navbar-light mb-3" style="background:  #FFFFFF" id="top">
  <div class="container">
    <%= link_to "单词本", root_path, class: "navbar-brand text-secondary" %>
    <button class="navbar-toggler" data-toggle="collapse" data-target="#navbarNav"><span class="navbar-toggler-icon"></span></button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
          </li>
        </ul>
        <ul class="navbar-nav ml-auto">
          <% if logged_in? %>
            <li class="nav-item"></li>
            <li class="nav-item dropdown">
              <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">
                <% if current_user.avatar.to_s.size == 0 %>
                  <img src="https://test.zhengjiajun.com/uploads/wechat/name/29/middle_avatar.png" class="rounded-circle"  width="25" height="25"/>
                <% else %>
                  <%= image_tag current_user.avatar.large.url, size: "25x25", class: "rounded-circle" %>
                <% end %>
              </a>
              <div class="dropdown-menu dropdown-menu-right">
                <%= link_to "基本设置", edit_user_path(current_user), class: "dropdown-item text-secondary" %>
                <div class="dropdown-divider"></div>
                <%= link_to "退出", logout_path, method: "delete", class: "dropdown-item text-secondary" %>
              </div>
            </li>
          <% else %>
            <li class="nav-item"><%= link_to "登录", login_path, class: "nav-link" %></li>
          <% end -%>
        </ul>
      </div>
  </div>
</nav>


